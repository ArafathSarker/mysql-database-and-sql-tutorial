# SQL Table Rename and Comments Example

This document demonstrates how to **rename a table** and how to use **comments** in SQL.  
It also includes an example of creating a database.

---

## 🧠 Rename a Table in SQL

You can rename an existing table using the `RENAME TABLE` statement:

```sql
RENAME TABLE old_name TO new_name;
